{
  "name": "podcast-monorepo",
  "private": true,
  "scripts": {
    "gen": "pnpm --filter generator start",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "deploy": "pnpm run build"
  },
  "pnpm": {
    "overrides": {
      "vite": "^7.3.1"
    },
    "onlyBuiltDependencies": [
      "@ffmpeg-installer/darwin-arm64",
      "esbuild",
      "node-pty",
      "protobufjs",
      "sharp",
      "tree-sitter-bash",
      "workerd"
    ]
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
